# 微信多开免扫码登录工具 需求规格说明书

**版本**：V2.2  
**日期**：2026-07-28  
**作者**：开发团队  
**状态**：已发布 v1.0.0

---

## 1. 项目概述

### 1.1 背景

微信 PC 4.x 版本将登录凭证存储在 `global_config` + `global_config.crc` 两个文件中（路径：`{数据目录}\xwechat_files\all_users\config\`）。微信启动后会**锁定这两个文件**，且同一时间只能有一个账号的凭证存在于该目录。后登录的账号会覆盖前者凭证，导致切换账号时必须重新扫码。

市面上已有成熟工具（如 solong 微信多开助手、BetterWX 等）解决了此问题，核心技术路线已验证可行。本项目的目标是自研一款**轻量、透明、可审计**的同类工具。

### 1.2 目标

- 在 **同一 Windows 账户** 中同时运行 **最多 4 个微信 4.x 客户端实例**（微信官方限制）。
- 每个账号 **首次登录时需扫码**，之后 **再次登录时无需扫码**，直接进入微信主界面。
- 提供现代化的图形界面（GUI），方便用户添加、删除账号，并一键启动指定账号。
- 各账号登录凭证相互隔离，互不干扰。

### 1.3 适用范围

| 项目 | 说明 |
|------|------|
| 操作系统 | Windows 10/11（64 位） |
| 微信版本 | **微信 4.x 系列**（不兼容 3.x） |
| 用户群体 | 运营人员、客服、测试人员等需同时管理多个微信账号的个人或团队 |
| 同时在线上限 | 最多 4 个账号（微信服务端限制） |

---

## 2. 功能需求

### 2.1 账号管理

| 功能编号 | 功能名称 | 详细描述 |
|:---------|:---------|:---------|
| FR-01 | 添加账号 | 用户创建"账号卡片"，输入自定义名称（如"客服1号"）。程序为该账号分配独立的凭证备份目录 |
| FR-02 | 删除账号 | 删除账号卡片，同时可选择是否删除对应的凭证备份数据 |
| FR-03 | 账号列表展示 | 主界面以卡片形式展示所有账号，显示头像首字母、名称、最后登录时间、凭证状态、在线状态 |
| FR-04 | 账号搜索 | 提供搜索框实时过滤账号（按名称和备注匹配） |

### 2.2 登录与多开

| 功能编号 | 功能名称 | 详细描述 |
|:---------|:---------|:---------|
| FR-05 | 一键启动 | 用户点击账号的"启动"按钮，程序自动：① 将该账号凭证通过链接映射到微信配置目录；② 以 `--multi` 参数启动 `Weixin.exe`；③ 完成后更新在线状态 |
| FR-06 | 免扫码登录 | 若该账号的 `global_config` + `global_config.crc` 凭证有效且未过期，微信启动后直接进入主界面，无需扫码。若凭证过期，弹出扫码窗口，用户扫码后可重新备份凭证 |
| FR-07 | 凭证备份 | 用户在微信扫码登录成功后，点击「备份凭证」，程序将微信当前使用的 `global_config` + `global_config.crc` 复制到该账号对应的独立备份目录。后续该账号即可免扫码 |
| FR-08 | 多实例运行 | 同时启动多个不同账号的微信实例。单账号使用**硬链接**（对微信完全透明）；多开使用**软链接**（微信 #1 锁定文件后仍可删除重建，微信 #2 读到自己的凭证）。启动参数 `--multi` 绕过互斥体单实例限制 |

### 2.3 凭证维护

| 功能编号 | 功能名称 | 详细描述 |
|:---------|:---------|:---------|
| FR-09 | 凭证状态显示 | 界面实时显示每个账号的凭证状态：已备份（绿色）、未备份（橙色） |
| FR-10 | 手动备份凭证 | 在账号已登录状态下，提供"备份凭证"按钮，将当前微信实例的凭证保存到该账号的备份目录 |
| FR-11 | 凭证导出/导入 | 支持将指定账号的凭证文件导出为 zip 压缩包，便于迁移到其他电脑；支持导入已有凭证备份 |

### 2.4 系统设置

| 功能编号 | 功能名称 | 详细描述 |
|:---------|:---------|:---------|
| FR-12 | 微信路径设置 | 允许用户手动指定 `Weixin.exe` 的安装路径（默认自动检测注册表及常见路径） |
| FR-13 | 数据存储位置 | 允许用户修改所有账号数据的根目录（默认 `%USERPROFILE%\Documents\WeChatMulti`） |
| FR-14 | 开机自启 | 可选是否随系统启动自动运行本工具（通过注册表 `HKCU\...\Run` 键实现） |
| FR-15 | 日志记录 | 记录关键操作日志（凭证切换、微信启动、错误等），支持日志级别设置和自动轮转（10MB × 5） |

---

## 3. 技术方案

### 3.1 架构概述

```
┌──────────────────────────────────┐
│     gui/index.html               │  ← HTML/CSS/JS 前端（现代化 UI）
│  卡片列表 / 搜索 / 模态框 / Toast  │
└──────────────┬───────────────────┘
               │ window.pywebview.api.xxx()
               │ 事件: pywebviewready
┌──────────────▼───────────────────┐
│     gui/api.py                   │  ← JS-Python 桥接层
│  13 个方法供前端异步调用          │
└──────────────┬───────────────────┘
               │
┌──────────────▼───────────────────┐
│     core/ （业务逻辑）            │
│  account_manager   账号 CRUD     │
│  credential_manager 凭证备份/切换 │
│  launcher          微信启动       │
│  process_detector  进程检测       │
└──────────────────────────────────┘
```

### 3.2 开发语言与依赖

| 项目 | 选型 |
|------|------|
| 语言 | Python 3.10+ |
| 桌面壳 | **pywebview 6.x**（调用系统 Edge WebView2 渲染 HTML） |
| 前端 | 单文件 HTML + CSS + 原生 JavaScript |
| 打包工具 | PyInstaller（打包为单文件 exe） |

**依赖库：**

| 库 | 用途 |
|----|------|
| `pywebview` | 桌面窗口容器，JS-Python 双向桥接 |
| `psutil` | 检测微信进程状态（运行中/已退出） |
| `pywin32` | Windows API 调用（注册表、窗口枚举、进程窗口关联） |
| `subprocess` | 启动微信进程、执行 `mklink` / `fsutil` 命令 |
| `os` | 硬链接创建（`os.link`） |
| `logging` | 日志记录 |
| `ctypes` | 管理员权限检测（`shell32.IsUserAnAdmin`） |

### 3.3 核心模块设计

| 模块 | 文件 | 职责 | 关键技术点 |
|:-----|:-----|:-----|:-----------|
| **账号管理** | `core/account_manager.py` | 账号卡片 CRUD，存储账号名、凭证路径、最后登录时间等 | JSON 文件存储（数据量小，结构简单） |
| **凭证管理** | `core/credential_manager.py` | ① 备份/恢复 `global_config` + `global_config.crc`；② 单账号硬链接切换（`os.link`）；③ 多开软链接切换（`mklink`） | 三级回退：`os.link` 硬链接 → `mklink /H` → `mklink` 软链接 |
| **微信启动** | `core/launcher.py` | 启动 `Weixin.exe --multi`，支持单开、并发多开、顺序多开 | `subprocess.Popen` + `--multi` 参数 |
| **进程检测** | `core/process_detector.py` | 检测微信进程运行状态、在线数量、可启动余量 | `win32gui.EnumWindows` 按可见窗口标题"微信"筛选主进程（排除 worker 子进程）；`psutil` 进程管理 |
| **JS 桥接** | `gui/api.py` | 暴露 13 个方法给前端，所有方法返回 JSON 可序列化数据 | 函数通过 `js_api` 注入，前端通过 `window.pywebview.api.xxx()` 异步调用 |
| **前端 UI** | `gui/index.html` | 账号卡片列表、搜索、模态框、Toast 提示、状态栏 | 原生 HTML/CSS/JS；CSS 变量主题；监听 `pywebviewready` 事件后初始化 |
| **配置管理** | `config/settings.py` | 管理微信路径、数据目录、开机自启、日志级别 | JSON 持久化；注册表 Run 键写入/删除 |
| **日志模块** | `utils/logger.py` | 记录操作日志，文件轮转 | `logging` + `RotatingFileHandler` |

### 3.4 关键技术流程

#### 3.4.1 首次使用——添加账号并备份凭证

```
1. 用户在微信中手动扫码登录账号 A
2. 用户在本工具中点击「添加账号」，输入名称（如"客服1号"）
3. 用户点击「备份凭证」
4. 程序将微信配置目录下的 global_config + global_config.crc
   复制到 {数据根目录}\credentials\acc_{id}\ 下
5. 账号 A 备份完成，此后可免扫码启动
6. 重复步骤 1-5 添加账号 B、C、D
```

#### 3.4.2 单账号启动（硬链接）

```
1. 用户点击账号 A 的「启动」
2. 程序检查凭证备份是否存在
3. 若存在 → os.link() 创建硬链接到微信配置目录
   （硬链接对微信完全透明，无法区分）
4. subprocess.Popen("Weixin.exe", "--multi") 启动微信
5. 微信读取硬链接 → 免扫码登录
```

#### 3.4.3 多开启动（软链接，关键区别）

```
1. 用户点击「一键启动全部」
2. 程序遍历已备份的账号：

   第一轮（账号 A）:
     a. mklink 创建软链接：global_config → acc_A 的备份
     b. Popen("Weixin.exe", "--multi")
     c. 等待 3 秒（确保微信 A 完成凭证读取）

   第二轮（账号 B）:
     d. 删除旧软链接（微信 A 持有底层文件句柄，不受影响）
     e. mklink 创建软链接：global_config → acc_B 的备份
     f. Popen("Weixin.exe", "--multi")
     g. 微信 B 通过新软链接读到账号 B 的凭证 ✅

   第三轮、第四轮同理...

   ★ 为什么多开不能用硬链接：
     硬链接 = 文件的另一个名字。微信 A 打开文件后 Windows 锁定底层数据，
     无法删除旧硬链接并创建新硬链接指向账号 B 的备份。
     
   ★ 为什么软链接可以：
     软链接 = 指向文件的指针。删除软链接不影响底层文件（微信 A 持有的
     文件句柄仍然有效）。新软链接可以指向不同账号的备份。
```

#### 3.4.4 凭证失效后的恢复

```
1. 用户启动账号 A，微信弹出扫码界面（凭证过期）
2. 用户扫码登录
3. 登录成功后，用户在本工具中点击「备份凭证」
4. 程序将新的 global_config 覆盖备份到账号 A 目录
5. 下次启动即可免扫码
```

### 3.5 关键路径与文件

#### 微信 4.x 凭证文件位置

```
{微信数据目录}\xwechat_files\all_users\config\
├── global_config          ← 登录凭证主文件（MMKV 格式）
├── global_config.crc      ← 凭证校验文件
└── ...（其他文件）
```

微信进程启动后会**以排他方式锁定**这两个文件，因此**无法直接复制替换**，必须使用链接方案。

#### 链接方案对比

| | 硬链接 (`os.link` / `mklink /H`) | 软链接 (`mklink`) |
|---|---|---|
| **透明度** | 完全透明，应用无法区分 | 可能被 `FILE_FLAG_OPEN_REPARSE_POINT` 拦截 |
| **文件锁定时** | 无法删除（底层数据被锁） | **可以删除**（只是指针，不影响底层文件） |
| **跨卷支持** | 不支持（必须同盘符） | 支持 |
| **管理员权限** | 不需要 | **需要** |
| **适用场景** | 单账号启动 | **多开** |

#### 凭证备份目录结构

```
{root_data_dir}\credentials\
  acc_a1b2c3d4\
    global_config          ← 账号 A 的凭证
    global_config.crc
  acc_e5f6g7h8\
    global_config          ← 账号 B 的凭证
    global_config.crc
```

### 3.6 数据结构

**账号配置存储**（`accounts.json`）：

```json
{
  "accounts": [
    {
      "id": "a1b2c3d4",
      "name": "客服1号",
      "credential_dir": "D:\\WeChatMulti\\credentials\\acc_a1b2c3d4",
      "last_login": "2026-07-28 10:30:00",
      "remark": "工作用"
    }
  ]
}
```

**设置存储**（`settings.json`）：

```json
{
  "wechat_exe_path": "E:\\Program Files\\Weixin\\Weixin.exe",
  "wechat_data_dir": "C:\\Users\\Administrator\\Documents",
  "root_data_dir": "C:\\Users\\Administrator\\Documents\\WeChatMulti",
  "auto_start": false,
  "log_level": "INFO"
}
```

### 3.7 进程检测原理

微信 4.x 是多进程架构（类似 Chrome），一个可见窗口会衍生多个 `Weixin.exe` 子进程。简单按进程名统计会将 1 个实例误判为 5 个。

**本工具的检测方式**：通过 `win32gui.EnumWindows` 枚举所有可见窗口 → 筛选标题为"微信"或窗口类名为 `Qt5*` 的窗口 → 反查窗口所属 PID → 确认进程名为 `Weixin.exe` → 仅计数这些 PID。

| 进程名 | 类型 | 是否计入 |
|--------|------|---------|
| `Weixin.exe`（有可见窗口） | 主实例 | ✅ 计入 |
| `Weixin.exe`（无可见窗口） | Worker 子进程 | ❌ 排除 |
| `WeChatAppEx.exe` | 小程序框架 | ❌ 排除 |

### 3.8 多开原理

微信 4.x 通过命名互斥体 `XWeChat_App_Instance_Identity_Mutex_Name` 限制单实例。本工具通过 `--multi` 启动参数绕过此限制，不再需要手动操作互斥体。

```python
subprocess.Popen(
    [wechat_path, "--multi"],
    cwd=wechat_dir,
    shell=False,
)
```

---

## 4. 用户界面设计

### 4.1 主界面布局

```
┌──────────────────────────────────────────┐
│  微信多开助手                    [⚙ 设置] │
├──────────────────────────────────────────┤
│  🔍 搜索账号...                          │
├──────────────────────────────────────────┤
│  ┌────────────────────────────────────┐  │
│  │ ● [A]  客服1号         ✓ 已备份   │  │
│  │        最后登录: 2026-07-28 10:30 │  │
│  │        [▶ 启动] [📋 备份] [🗑]   │  │
│  └────────────────────────────────────┘  │
│  ┌────────────────────────────────────┐  │
│  │ ○ [B]  客服2号         ⚠ 未备份   │  │
│  │        从未登录                   │  │
│  │        [▶ 启动] [📋 备份] [🗑]   │  │
│  └────────────────────────────────────┘  │
├──────────────────────────────────────────┤
│  就绪 | 已添加 4 个账号 | 微信进程: 2/4  │
│  [⏹ 关闭微信] [▶ 一键启动全部] [+ 添加] │
└──────────────────────────────────────────┘
```

### 4.2 视觉设计

| 元素 | 设计 |
|------|------|
| **配色** | 主色 `#4f46e5`（靛蓝），成功 `#10b981`，警告 `#f59e0b`，危险 `#ef4444` |
| **卡片** | 白色背景，圆角 12px，阴影 hover 时抬升 |
| **头像** | 渐变色圆形，显示账号名首字母 |
| **状态灯** | 在线绿色发光，离线灰色 |
| **Toast** | 右上角弹出，3 秒自动消失，成功绿/失败红 |

### 4.3 模态框

- **添加账号**：名称输入 + 备注输入 + 操作提示 + 确认/取消
- **设置**：路径展示 + 浏览按钮 + 开机自启复选框 + 日志级别下拉 + 保存/取消

---

## 5. 非功能需求

| 类别 | 要求 |
|:-----|:-----|
| **性能** | 凭证链接切换应在 **500ms 内**完成；单次微信启动全流程应在 **1 秒内**（不含微信自身加载时间）；多开时每个实例间隔 3 秒确保凭证正确读取 |
| **兼容性** | 仅支持微信 4.x 系列；进程名适配 `Weixin.exe`；窗口类名适配 `Qt5*` 前缀；凭证文件路径和名称做成配置项 |
| **安全性** | 凭证文件包含敏感信息，备份目录应仅当前用户可读写；不将凭证明文上传或网络传输；不修改微信进程内存 |
| **稳定性** | 程序自身不应频繁崩溃；微信异常退出时，程序应能检测并更新状态 |
| **易用性** | 界面简洁直观，无需专业知识即可操作；Toast 即时反馈；搜索实时过滤 |
| **可维护性** | 代码模块化（前端 / 桥接 / 业务三层分离）；关键参数（互斥体名、进程名、路径）抽取为配置项 |
| **日志记录** | 日志文件最大 10MB，自动轮转保留最近 5 个；记录操作、错误、警告 |

---

## 6. 风险与限制

| 风险/限制 | 说明 | 缓解措施 |
|:----------|:-----|:---------|
| **微信版本更新** | 微信升级可能改变进程名、窗口特征、凭证文件格式 | 关键参数配置化；关注社区更新 |
| **账号安全风险** | 工具操作微信登录凭证，可能被风控识别 | 仅做本地文件链接，不篡改进程；建议非关键账号使用 |
| **凭证过期** | 微信登录凭证有时效性（数天到数周） | 界面标记凭证状态；提供快捷备份操作 |
| **同时在线上限** | 微信服务端限制单机最多 4 个账号 | 界面实时显示余量；超限拒绝启动 |
| **管理员权限依赖** | `mklink` 软链接需要管理员权限 | 启动时自动检测并以 `runas` 提权 |
| **WebView2 依赖** | pywebview 依赖系统 Edge WebView2 运行时 | Windows 11 内置；Win10 未安装时提示下载 |
| **多开文件锁** | 硬链接在文件被锁定时无法删除 | 多开场景自动使用软链接 |

---

## 7. 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| **硬链接（Hard Link）** | `os.link()` / `mklink /H`。为同一文件数据创建额外的目录条目，应用完全无法区分。同卷限制，文件锁定时无法删除 |
| **软链接（Symbolic Link）** | `mklink`。指向目标文件的指针（重解析点），可在文件被锁定时删除重建。需管理员权限 |
| **登录凭证** | 微信服务器下发的身份验证数据，存储于 `global_config` + `global_config.crc`（MMKV 格式） |
| **互斥体（Mutex）** | Windows 内核对象，微信用于确保单实例运行。4.x 名称：`XWeChat_App_Instance_Identity_Mutex_Name` |
| **pywebview** | Python 桌面壳框架，调用系统 WebView2 渲染 HTML，提供 JS-Python 双向通信 |
| **pywebviewready** | pywebview 6.x 注入 JS API 完成后触发的 DOM 事件，在此之前 `window.pywebview.api` 不可用 |

### B. 微信 4.x 关键路径速查

| 项目 | 路径 / 值 |
|------|-----------|
| 微信主程序 | `Weixin.exe`（4.x 改名，旧版为 `WeChat.exe`） |
| 进程名 | `Weixin.exe`（主进程 + worker 子进程同名） |
| 安装目录 | 注册表 `HKLM\SOFTWARE\...\Uninstall\WeChat\InstallLocation` |
| 默认安装路径 | `C:\Program Files\Tencent\Weixin\` |
| 数据目录 | 由用户在微信中指定，常见 `Documents` 或自定义 |
| 凭证路径 | `{数据目录}\xwechat_files\all_users\config\` |
| 凭证文件 | `global_config`、`global_config.crc` |
| 互斥体名称 | `XWeChat_App_Instance_Identity_Mutex_Name` |
| 窗口类名 | `Qt51514QWindowlcon`（Qt5 前缀） |
| 启动参数 | `--multi`（绕过单实例限制） |

### C. 项目文件结构

```
wechat-account/
├── main.py                         # 入口：管理员提权 → 初始化 → pywebview GUI
├── requirements.txt                # pywebview / psutil / pywin32
├── wechat_multi.spec               # PyInstaller 打包配置（含 UAC 清单）
│
├── config/
│   └── settings.py                 # 配置管理（路径探测 / 注册表自启 / JSON 持久化）
│
├── core/
│   ├── account_manager.py          # 账号 CRUD（JSON 存储 / 搜索 / 排序）
│   ├── credential_manager.py       # 凭证管理（备份 / 硬链接 / 软链接 / 导出导入）
│   ├── launcher.py                 # 微信启动器（--multi / 单开 / 顺序多开）
│   └── process_detector.py         # 进程检测（窗口枚举 / 主进程区分 / 上限检查）
│
├── gui/
│   ├── api.py                      # JS-Python 桥接（13 个 API 方法）
│   └── index.html                  # 前端界面（卡片列表 / 搜索 / 模态框 / Toast）
│
├── utils/
│   └── logger.py                   # 日志（RotatingFileHandler / 10MB 轮转）
│
├── data/                           # 运行时数据（账号 JSON、凭证备份）
└── logs/                           # 日志文件
```

### D. 参考资料

- 微信 PC 版逆向分析相关论坛（吾爱破解、看雪）
- solong 微信多开助手（52pojie.cn/thread-2051461-1-1.html）
- BetterWX 项目（github.com/zetaloop/betterwx）
- pywebview 官方文档（pywebview.flowrl.com）
- Python `ctypes` / `subprocess` 官方文档

---

**文档变更记录：**

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| V1.0 | 2026-07-28 | 初稿，涵盖 3.x + 4.x |
| V2.0 | 2026-07-28 | 聚焦 4.x；修正凭证方案为 mklink 软链接；更新路径、文件名 |
| V2.1 | 2026-07-28 | GUI 框架从 PyQt5 改为 pywebview + HTML/CSS；新增硬链接/软链接双模式；进程检测改为窗口枚举法；启动参数增加 `--multi`；更新文件结构 |
| V2.2 | 2026-07-28 | 打包方案从 PyInstaller→Nuitka→cx_Freeze→最终 PyInstaller（去掉 UAC manifest 解决 SxS 错误）；自定义无边框标题栏；卡片 hover 抽屉动画；图标按钮；头像/wxid 自动检测；相对时间显示；发布 v1.0.0 |
